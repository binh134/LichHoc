import { Search, Plus, Filter, Upload, MapPin } from "lucide-react";
import { PageHeading, Button, Badge } from "../components/AppShell";
import { Stat } from "../components/Display";
import { classes } from "../data/mockSchedule";

export function ManagePage() { return <><PageHeading eyebrow="DANH SÁCH HỌC PHẦN" title="Quản lý lịch học" detail="Theo dõi, sắp xếp và cập nhật các lớp học trong học kỳ của bạn." action={<Button icon={Plus}>Thêm môn học</Button>} /><div className="stat-grid compact-stats"><Stat label="Tổng số lớp" value="6" note="Trong học kỳ hiện tại" icon={BookOpen} tone="stat-blue" /><Stat label="Tín chỉ đăng ký" value="18" note="Trên tổng 24 tín chỉ" icon={CalendarCheck} tone="stat-green" /><Stat label="Buổi học sắp tới" value="12" note="Trong 7 ngày tiếp theo" icon={Clock3} tone="stat-amber" /></div><section className="panel table-panel"><div className="table-toolbar"><div className="search-box table-search"><Search size={16} /><input placeholder="Tìm theo môn học, giảng viên, phòng..." /></div><Button variant="outline" icon={Filter}>Bộ lọc</Button><select aria-label="Học kỳ"><option>Học kỳ 1 · 2025–2026</option></select><Button variant="outline" icon={Upload}>Xuất dữ liệu</Button></div><div className="table-scroll"><table><thead><tr><th>MÔN HỌC</th><th>MÃ LỚP</th><th>LỊCH HỌC</th><th>PHÒNG</th><th>GIẢNG VIÊN</th><th>TRẠNG THÁI</th><th></th></tr></thead><tbody>{classes.map((c, i) => <tr key={c.code}><td><div className="course-cell"><i className={`course-mark ${c.tone}`} /><span><b>{c.name}</b><small>{[3, 3, 3, 3, 4, 2][i]} tín chỉ</small></span></div></td><td><span className="code-pill">{c.code}</span></td><td>{c.day} · {c.time}</td><td><MapPin size={14} />{c.room}</td><td>{c.teacher}</td><td><Badge tone={i === 0 ? "amber" : "green"}>{c.status}</Badge></td><td><div className="row-actions"><button aria-label="Xem"><Eye size={16} /></button><button aria-label="Sửa"><Pencil size={16} /></button><button aria-label="Xóa"><Trash2 size={16} /></button></div></td></tr>)}</tbody></table></div><div className="table-footer">Hiển thị <b>1–6</b> trong <b>6</b> lớp học <div><button>‹</button><button className="current-page">1</button><button>›</button></div></div></section></> }
export default ManagePage;
